automation:
  image: debian:stable-slim
  before_script:
    - apt-get update -qq
    - apt-get install -y --no-install-recommends \
        git make ca-certificates curl findutils jq squashfs-tools xz-utils
  script:
    - ./automation.sh
